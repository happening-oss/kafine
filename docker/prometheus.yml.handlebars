global:
  scrape_interval: 5s
scrape_configs:
  - job_name: "kafka"
    static_configs:
      - targets:
{{#each clusters}}
{{#each brokers}}
          - kafka-{{../name}}-{{id}}:9102
{{/each}}
{{/each}}
  - job_name: "host"
    static_configs:
      - targets:
        - host.docker.internal:8099
